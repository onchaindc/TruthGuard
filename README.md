# TruthGuard

TruthGuard is an AI-powered fact checker and content verifier for GenLayer Bradbury Testnet.

Submit a claim with optional evidence URLs. The app simulates a GenLayer intelligent-contract flow where multiple AI validators fetch evidence, analyze the claim, and reach a consensus verdict.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.
